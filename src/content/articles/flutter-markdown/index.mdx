---
title: 'FlutterでMarkdownのテキストを表示する'
description: ''
publishedDate: 2021-06-13
tags: ['Flutter', 'Dart']
image: '../../../assets/images/articles/flutter.png'
---

FlutterでMarkdownのテキストを表示する為には、flutter_markdownを利用します。

```yaml title="pubspec.yaml" ins={2}
dependencies:
  flutter_markdown: ^0.6.2
```

```dart title="main.dart"
import 'package:flutter/material.dart';
import 'package:flutter_markdown/flutter_markdown.dart';

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        appBar: AppBar(title: Text("MarkdownDemo")),
        body: Markdown(data: markdownText),
      ),
    );
  }
}

final String markdownText = '''
# 見出し1
hoge

## 見出し2
fuga

### 見出し3
piyo 
'''
```
