import { createMuiTheme } from '@material-ui/core'

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#000',
    },
    secondary: {
      main: '#6441a5',
    },
    background: {
      default: '#f7f7fc',
    },
  },
});

export default theme